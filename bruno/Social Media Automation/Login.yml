info:
  name: Login
  type: http
  seq: 7

http:
  method: POST
  url: http://127.0.0.1:8000/auth/login
  body:
    type: json
    data: |-
      {
        "email": "admin@test.com",
        "password": "123456"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
