info:
  name: MCP Publish
  type: http
  seq: 4

http:
  method: POST
  url: http://127.0.0.1:8000/mcp/publish
  body:
    type: json
    data: |-
      {
          "message":"Launching our new AI Startup"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
